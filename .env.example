# ================================================================
# 성지DROP — 환경변수 예시 파일
# ================================================================
# 이 파일을 복사해서 .env 파일을 만드세요:
#   cp .env.example .env
#
# .env 파일은 .gitignore 에 포함되어 있어 커밋되지 않습니다.
# Expo는 EXPO_PUBLIC_ 접두사가 붙은 변수를 클라이언트 번들에 포함합니다.
# ================================================================

# Supabase 프로젝트 URL (Dashboard > Settings > API > Project URL)
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Supabase anon key (Dashboard > Settings > API > Project API keys > anon/public)
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
